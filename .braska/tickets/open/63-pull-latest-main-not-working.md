# Pull Latest Main Not Working Properly

## Priority: High

## Description
The "pull latest main" functionality is not working properly. This feature is used to pull the latest changes from the main branch (typically into a worktree). The exact failure mode needs to be investigated and fixed.

## Tasks
- Investigate the current pull latest main implementation and identify the bug
- Fix the issue so pulling latest main works correctly
- Test the fix across relevant scenarios (worktree branches, clean/dirty working trees)
