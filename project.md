# The Agency — Project Log

## Progress

- **2026-03-28**: Scaffolded boilerplate Electron app (main process, preload script, renderer HTML with dark theme, CSP headers)
- **2026-03-28**: Added side menu with project management — add folders via native dialog, remove with hover button, persisted to JSON in userData
- **2026-03-28**: Git repo detection for sidebar projects — orange branch icon badge, collapsible worktree accordion (including main) under each git project, chevron absolutely positioned so all project names align flush
- **2026-03-28**: Added settings view — cog button in sidebar footer, toggles sidebar to settings menu (Experts, Skills, MCP Servers as nav buttons), selected section content opens in main panel with header + add button, back arrow returns to project list
- **2026-03-28**: Skills management — add/edit/remove skills stored as `.md` files in `~/.the-agency/skills/` (auto-created on first use), name + prompt content form in main panel, click to edit, hover X to delete
- **2026-03-28**: Experts management — create experts as folders in `~/.the-agency/experts/<name>/`, instructions stored in `claude.md`, skills assigned via symlinks from `.claude/skills/<skill-name>` to `~/.the-agency/skills/<skill-name>.md`, form with name + instructions textarea + skill checkboxes
- **2026-03-28**: Task launching — click worktree (or non-git project) → expert picker modal → embedded xterm.js terminal in main panel, spawns `claude --dangerously-skip-permissions` from expert folder (picks up `claude.md` + skills) with initial prompt to work in the selected worktree/project directory. Uses `node-pty` for real PTY in main process, preload exposes thin pty IPC bridge (spawn/write/resize/kill + data/exit callbacks), xterm.js loaded in renderer via ESM dynamic `import()` for full DOM access.
- **2026-03-28**: Multi-tab terminals scoped per worktree — tabs belong to their worktree/project. Clicking a worktree in sidebar switches to its tabs (or shows expert picker if none). Tab bar shows only the active worktree's tabs with expert names, "+" button to add more experts. Closing last tab for a worktree returns to main view. All PTY processes stay alive in background when switching between worktrees.
- **2026-03-28**: File tree explorer panel on right side — shows current worktree's files with lazy-loaded expandable directories (sorted dirs-first). Toggle via toolbar buttons or keyboard shortcuts (⌘B sidebar, ⌘E explorer). Content area wrapped in `#content-area` with thin toolbar strip containing panel toggle icons (panel-left/panel-right SVGs). File tree reads directory contents via `filetree:list` IPC handler.
- **2026-03-29**: Removed node_modules filter from file tree explorer — all directory entries now shown
- **2026-03-29**: Fixed `@` file references in expert sessions — added `--add-dir` flag to claude spawn command so experts can reference project files while still loading config from expert folder
- **2026-03-29**: Code review and ticket tracking — created `tickets/` folder with 14 tracked tickets covering security (directory traversal, command injection, XSS, hardcoded `--dangerously-skip-permissions`), architecture (extract CSS/JS from monolith HTML, add bundler, async I/O), reliability (error handling, memory leaks), and project hygiene (tests, metadata, versioning)
- **2026-03-29**: Fixed expert initial prompt being swallowed by `--add-dir` — the `--add-dir` flag accepts variadic arguments (`<directories...>`), so the positional prompt was consumed as an extra directory. Added `--` separator before the prompt argument to fix
- **2026-03-29**: Launchpad view when clicking a worktree with no open tabs — instead of immediately showing the expert picker modal, displays a centered launchpad with two cards: "Get an Expert" (opens expert picker) and "Start a Terminal" (spawns a plain shell session). Closing the last tab for a worktree returns to the launchpad rather than the home screen. File explorer works from the launchpad since `activeWorkDir` is set on click.
- **2026-03-29**: Active context indicators — toolbar breadcrumb shows `project / branch` (or project name for non-git) in `#content-toolbar`, clears when returning to home screen. Selected worktree item in sidebar gets a blue left-edge highlight (`box-shadow: inset 2px 0 0 #4a9eff`). Both update together via `setBreadcrumb()` called from `openWorkDir()`.
- **2026-03-29**: Tab "+" button now shows a "New Tab" picker modal with two choices — "Get an Expert" (proceeds to expert picker) or "Start a Terminal" (spawns plain shell directly) — matching the launchpad card pattern instead of going straight to the expert picker
- **2026-03-29**: Browser tabs — third option "Open a Browser" in launchpad and new-tab picker. Opens an embedded Chromium browser tab via Electron `<webview>` (enabled with `webviewTag: true`). Starts blank with focus on URL bar; nav bar has back/forward/reload buttons + URL input. Tab title auto-updates from page title. Browser tabs tracked alongside terminal tabs with `type: 'browser'` field; `switchTab`/`closeTab` handle both types.
- **2026-03-29**: Inline file editor — clicking a file in the explorer opens it in a new editor tab (`type: 'editor'`). Uses textarea with monospace font matching terminal theme. Features: dirty indicator (● prefix in tab title), ⌘S save, Tab key inserts spaces, duplicate-open prevention (switches to existing tab). IPC handlers `file:read`/`file:save` in main process, exposed via `fileEditor` preload bridge.
- **2026-03-29**: Git diff view — right panel now has Explorer/Changes toggle tabs (⌘D). Changes tab shows staged, unstaged, and untracked files with VSCode-style status badges (M/A/U/D colored letters), filename + dimmed folder path display, section count pills, and +/- line stats. Recent commits listed below (collapsible to show per-file stats). Clicking any changed file opens a `type: 'diff'` tab with **side-by-side diff** (old left, new right) including word-level inline highlighting via LCS-based tokenizer (`diff-word-add`/`diff-word-del` spans), hunk headers, and empty-cell padding for unpaired adds/deletes. IPC handlers `git:status`, `git:diff`, `git:log`, `git:commit-files`, `git:diff-commit` in main process, exposed via `gitDiff` preload bridge. Duplicate-open prevention for diff tabs.
- **2026-03-29**: Launchpad and new-tab picker polish — shortened card/item labels to single words (Expert, Terminal, Browser), added "Open a new tab containing:" heading to both launchpad and new-tab picker modal, fixed launchpad cards to uniform width
- **2026-03-29**: File drag-and-drop onto terminal panes — global drag-drop prevention (stops Electron from navigating to dropped files), per-pane drag overlay with dashed blue border highlight, drop handler extracts file paths via `webUtils.getPathForFile()` in preload capture phase (since `File.path` is deprecated in Electron 29+ and broken in 41), passes paths across context bridge as strings. Claude/expert sessions get `@`-prefixed paths so Claude Code attaches files; plain terminal sessions get bare paths. Handles spaces in paths via quoting.
- **2026-03-29**: Cmd+W closes active tab instead of window — intercepted via `before-input-event` in main process (fires before Electron's native menu accelerator), sends IPC to renderer which calls `closeTab(activeTabId)`. Window stays open; if no tab is active, keystroke is simply ignored.
- **2026-03-29**: Right pane open by default — file explorer / git changes panel now visible on launch and when selecting a worktree, toggle button starts in active state. Minimum width enforced at 250px via existing `min-width` CSS.
- **2026-03-29**: Fixed file explorer collapsing on new tab — `startTask` and `startBrowser` were calling `refreshRightPanel` unconditionally, which rebuilt the entire file tree (`innerHTML = ''`) and collapsed all expanded directories. Now only refreshes when the workDir actually changes. Also added per-worktree explorer visibility state (`workDirExplorerVisible` map) so toggling the panel in one worktree doesn't affect others.
- **2026-03-29**: Ticket tracker open/closed folders — reorganised `tickets/` into `tickets/open/` and `tickets/closed/` subdirectories. Existing tickets moved to `open/`, resolved tickets go to `closed/`.
- **2026-03-29**: Resizable side panels — both sidebar (left) and explorer/changes panel (right) are now drag-resizable via thin handles on their inner edges. Handle highlights blue on hover/drag, `col-resize` cursor, minimum width 180px for both panels. Widths persist to `localStorage` across tab switches and app restarts. Handles auto-hide when their panel is collapsed/hidden. `body.resizing` class disables text selection and iframe pointer events during drag.
- **2026-03-29**: Visual notification system — two-part notification system for background activity. (1) Sidebar badges: blue dot on worktree items, project entries (including collapsed git projects which aggregate from all child worktrees), and individual tabs when a background terminal finishes (1s debounce after output stops, or immediate on process exit). Dots clear only when the specific tab is viewed — worktree/project dots persist until all child tab notifications are cleared. (2) Central notification bell in toolbar with unseen count badge. Clicking opens a dropdown listing recent notifications (tab name, project/branch context, output snippet, relative timestamp). Each entry is clickable to jump directly to that tab. "Clear all" button resets everything. Dropdown closes on outside click or Escape. Notification log capped at 50 entries, with per-tab deduplication. `activeTabId` set before PTY `onData` registration to prevent false notifications from startup output.
- **2026-03-29**: Splash screen — glassmorphism startup screen shown while the main window loads. Frameless transparent 340x340 window with frosted glass card (`backdrop-filter: blur(40px)`), drifting color orbs (blue/purple/green) behind the glass, diagonal light refraction sweep (alternating direction), spinning conic-gradient icon ring, and translucent loading bar. Main window uses `show: false` + `ready-to-show` with `backgroundColor: '#1e1e1e'` to eliminate white flash. Splash fades out with scale-down animation (0.4s) before main window appears.
- **2026-03-29**: Splash-to-app morph transition — splash card is now opaque (`#1e1e1e` base, no `backdrop-filter`), window has `hasShadow: false` to remove macOS frame. On ready-to-show, the splash morphs into the app: glass card expands to fill window (`position: fixed; inset: 0`), content/orbs/effects fade out, border-radius transitions from 32px to 10px, while JS animates `splash.setBounds()` to match the main window dimensions over 500ms (ease-out cubic, 30 steps). Finishes with a 200ms cross-fade (splash fades out, app fades in) over a solid `#1e1e1e` backdrop window to prevent desktop bleed-through.
- **2026-03-29**: Live file explorer & changes panel — added `fs.watch` (recursive) in main process to watch the active worktree directory. Watcher sends `filetree:changed` events via IPC to renderer, which debounces (300ms) and auto-refreshes whichever right panel tab is active (explorer or git changes). Watcher starts on `openWorkDir()` and is replaced when switching worktrees (single watcher at a time). Preload exposes `watch`/`unwatch`/`onChange` on the `filetree` bridge.
- **2026-03-29**: npm publishing via `npx` — added `bin/cli.js` executable script that spawns Electron with the app, added `"bin"` field to `package.json`, moved `electron` from devDependencies to dependencies so end users get it installed. Published v1.0.0 to npm registry — app is now installable via `npx the-agency`.
- **2026-03-29**: Busy/done indicators for Claude tabs — green pulsing dot on tab label, sidebar worktree entry, and parent project entry while a Claude/expert session is actively producing output in the background. Transitions to the existing static blue notification dot when output stops (1s idle debounce). Green takes priority over blue when both states exist. Only tracks background tabs (not the active tab the user is viewing) to avoid false triggers from terminal resize redraws. Content filtering via improved `stripAnsi` (now handles full CSI spec including private-mode sequences like `\x1b[?25h`, ESC+char sequences, and DEL) ensures only meaningful text output triggers the busy indicator. Safety guard in `markTabActivity` prevents notifications for the currently viewed tab. Busy state cleared on tab switch and tab close.
- **2026-03-29**: Per-project ticket tracking — tickets stored as markdown files in `<project>/.the-agency/tickets/open/` and `closed/` with format: `# Title`, `## Priority`, `## Description`, `## Tasks`. System expert `ticketmaster` auto-created in `~/.the-agency/experts/ticketmaster/` on startup with `claude.md` containing ticket format instructions (green-highlighted in picker) — scans existing files for next number, asks user what to create, writes the file. "Ticketmaster" card in launchpad and new-tab picker for one-click access. Tickets tab in right panel (⌘I) shows open/closed tickets with number badges, priority pills (color-coded High/Medium/Low), filter input, and click-to-view detail. "Work on this ticket..." button on detail view opens expert picker — selected expert starts with initial prompt to read the ticket file first. `startTask` and `pty:spawn` extended with `initialPrompt` parameter for prompt override. Auto-refreshes via existing `fs.watch` infrastructure.
- **2026-03-29**: Expert spawn refactor for `@` file tagging and skills — `@` autocompletion only searches CWD, so expert sessions now run with `cwd = workDir` (project directory) instead of the expert folder. Expert config loaded via `--add-dir '<expertDir>'` + `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` env var (loads `claude.md` from the `--add-dir` directory). Skills auto-load from `--add-dir` directories. No default initial prompt passed — Claude already knows its role from `claude.md` and its location from CWD; `-- '<prompt>'` only appended when an explicit `initialPrompt` is provided (work-on-ticket handoff). Expert skill storage changed from flat file symlinks to proper Claude Code skill directory structure: each skill is a directory (`<skill-name>/SKILL.md`) inside `.claude/skills/`, with `SKILL.md` symlinked to `~/.the-agency/skills/<name>.md`. This preserves full skill functionality (subdirectories, templates, scripts, argument substitution). `experts:save` handler does a full reconcile on every save — clears all skill directories and recreates from the checked skill list. `listExperts` reads skill directories instead of flat files. Symlink `.md` extension bug also fixed (previously created without `.md`).
- **2026-03-29**: Ticket number in tab labels — when starting work on a ticket via the expert picker, the tab label now shows the ticket number prefix (e.g. `#35 - Claude` instead of just `Claude`). Ticket number extracted from the filename and passed through `startTask` options.
- **2026-03-29**: Removed hardcoded `initialPrompt` from ticketmaster spawn — moved all runtime instructions (directory paths, startup workflow) into the expert's `claude.md` using relative paths, so ticketmaster now launches the same way as every other expert (no special-case prompt). `initialPrompt` parameter still used only for the work-on-ticket handoff.
- **2026-03-29**: Renamed issue system to tickets — full terminology rename across the codebase. Directory `.the-agency/issues/` → `.the-agency/tickets/`, expert `issue-creator` → `ticketmaster`, IPC channels `issues:*` → `tickets:*`, preload bridge `window.issues` → `window.tickets`. All CSS classes, JS functions, HTML elements, and UI labels updated (`refreshIssues` → `refreshTickets`, "New Issue" → "Ticketmaster", "Issues" tab → "Tickets", etc.). Launchpad and new-tab picker items now say "Ticketmaster" instead of "New Issue". Ticket file content updated where it referenced the old system terminology. All 33 existing ticket files verified intact after the move.
- **2026-03-29**: Worktree management — full CRUD for Git worktrees from the UI. Sidebar worktree list now shows lock icons and an "Add worktree" button per git project. **Create**: modal with "Create new branch" (default) or existing branch picker, auto-generated path using `../<repo>.worktrees/<branch>` convention (VS Code style). **Delete**: right-click context menu → "Remove Worktree" opens confirmation modal with "Also delete the branch" checkbox (checked by default); two-step escalation for dirty/locked trees (warns, then offers force remove); closes all associated tabs on removal. **Lock/Unlock**: context menu toggle per worktree. **Prune**: context menu action to clean stale entries. Main worktree protected from deletion and locking. `getGitInfo` enhanced to report `isMain`, `isLocked`, `isPrunable`. Branch selector filters out already-checked-out branches. Sidebar preserves expanded project state across re-renders. IPC handlers: `git:worktree-add`, `git:worktree-remove` (with branch deletion), `git:worktree-prune`, `git:worktree-lock`, `git:branches`. Preload `worktree` bridge exposes all operations.
- **2026-03-29**: Merge & Clean Up — right-click context menu action on non-main worktrees to merge the feature branch into main and clean up in one step. Pre-flight check shows branch summary, commit list, and dirty/locked/already-merged status. On confirm: merges feature branch into main (executed from main worktree directory), removes worktree, deletes branch, closes all associated tabs. Handles merge conflicts gracefully (auto-aborts, no changes made). "Already merged" variant offers clean-up-only flow. Partial success handling when merge works but cleanup fails. IPC handlers: `git:merge-preflight`, `git:merge-and-cleanup`. Modal with loading → ready → merging → done/error states, blue confirm button, scrollable commit list with green hashes.
- **2026-03-29**: Ticket closing with Done/Cancelled — replaced single "Close ticket" button with two buttons: green **Done** and red **Cancelled**. Tickets move to `tickets/done/` or `tickets/cancelled/` folders respectively. Ticket list shows three sections (Open, Done, Cancelled) — done tickets are dimmed, cancelled tickets are dimmed + struck through. `tickets:close` IPC handler accepts a `status` parameter. Legacy `tickets/closed/` folders auto-migrate to `tickets/done/` on access. Ticketmaster instructions updated to reference new folder names.
- **2026-03-29**: Work on ticket in new worktree — green "Work on this in a new worktree" button on ticket detail view (git projects only, open tickets). One-click flow: auto-generates branch name from ticket number + slugified title (`ticket-123-implement-dark-mode`), creates worktree using existing `../<project>.worktrees/<branch>` convention, refreshes sidebar, selects new worktree, then chains into expert picker with ticket context. Button shows "Creating worktree..." loading state, errors displayed inline. Helper `getProjectRootForWorkDir()` resolves git project root from any worktree via sidebar DOM traversal. No new IPC — reuses `window.worktree.add()`, `loadProjects()`, `openWorkDir()`, `showExpertPickerForTicket()`.
- **2026-03-29**: Fixed tickets panel infinite refresh loop — `getTicketsDir()` was calling `fs.mkdirSync()` on every `listTickets()` call, which triggered the recursive `fs.watch`, which re-triggered `refreshTickets()` in a loop. Fix: split `getTicketsDir()` into pure path function + one-time `ensureTicketsDirs()` (called via new `tickets:init` IPC on first tab open), filtered `fs.watch` events by filename so only `.the-agency/tickets/` changes trigger ticket refreshes, removed "Loading..." innerHTML flash, and added scroll position preservation across refreshes.
- **2026-03-29**: Auto-generate commit message — "Generate" button (sparkle icon) in the Changes panel commit area. Calls `claude -p --model claude-haiku-4-5-20251001 --max-turns 1` via `exec` with the user's login shell, pipes the staged diff (truncated to 20K chars) as prompt, populates the commit textarea with the result. Button enabled only when staged files exist, shows spinning icon + "Generating…" during the call. IPC handler `git:generate-commit-msg` in main process, `generateCommitMsg` on `window.gitOps` preload bridge.
- **2026-03-29**: Fixed unstage failing for renamed files — `git diff --cached --numstat` uses rename detection by default, outputting compact paths like `{open => closed}` which aren't real filesystem paths. Added `--no-renames` to both `git diff` commands in the `git:status` handler so renames appear as separate delete + add entries with valid paths.
- **2026-03-29**: Full git operations support — added 16 new IPC handlers for mutating git operations, exposed via new `window.gitOps` preload bridge alongside extended `window.gitDiff` bridge. **Staging**: stage/unstage individual files via +/− buttons on file entries, plus "± All" buttons on section headers to stage/unstage entire sections. **Commit**: inline commit message textarea + button in Changes panel, enables only when staged files exist and message is non-empty. **Network ops**: fetch/pull/push toolbar buttons with 30s timeouts; push auto-detects missing upstream and offers `--set-upstream origin/<branch>` via confirm dialog. **Stash**: toolbar button to stash all changes (including untracked via `-u`), stash list rendered between Untracked and Recent Commits sections with per-entry Pop/Drop buttons. **Branch management**: toolbar button opens modal listing all branches with current/worktree badges, tracking remote, ahead/behind counts; create new branches, switch (worktree-aware — prevents checkout of branches in other worktrees), delete with force-delete prompt for unmerged branches. Changes panel restructured: `#changes-body` wrapped in `#changes-panel-wrapper` containing toolbar row and commit area above the scrollable file list. Inline status messages (green success / red error) auto-fade after 4 seconds.
- **2026-03-29**: Master Debugger system expert — new built-in expert `debugger` auto-created on startup in `~/.the-agency/experts/debugger/` alongside ticketmaster. Follows a rigorous 6-step debugging methodology (Reproduce → Gather Context → Isolate → Identify Root Cause → Fix → Verify) with common bug pattern checklist and working principles. Both system experts (ticketmaster, debugger) now appear in the regular expert picker list instead of having dedicated launchpad cards — removed launchpad cards, tab-type-picker entries, and their click handlers. Legacy `issue-creator` expert filtered from the picker.
- **2026-03-29**: Code Reviewer system expert — new built-in expert `code-reviewer` auto-created on startup in `~/.the-agency/experts/code-reviewer/`. Read-only expert that reviews code without modifying files. On start, asks what to review (staged/unstaged changes, branch diff, specific files, commit range). Examines all changes across 8 categories (correctness, security, performance, code quality, error handling, style, testing, documentation). Produces structured output with severity-categorised findings (Critical Issues, Warnings, Suggestions, Looks Good) — each citing file paths and line numbers with code snippets and suggested fixes. Expert picker now distinguishes system vs custom experts: "System" section (muted blue, includes CLAUDE + built-in experts) separated from "Custom" section by divider with section labels. `listExperts()` returns `builtin` flag per expert via `BUILTIN_EXPERTS` array. Settings panel hides built-in experts from the editable list.
- **2026-03-29**: Fixed worktree-from-ticket failing on retry — `git:worktree-add` handler now catches "already exists" errors (branch or path) and retries gracefully: cleans up leftover worktree directory via `git worktree remove --force`, then re-attempts with the existing branch instead of `-b`.
- **2026-03-30**: Ticketmaster file-edit hook enforcement — hard restriction preventing the ticketmaster expert from editing files outside `.the-agency/tickets/`. `ensureSystemExperts()` now creates `.claude/settings.json` with a `PreToolUse` hook matching `Edit|Write` tools, and `.claude/hooks/protect-files.sh` (chmod 755) that reads the file path from tool input via `jq` and blocks any edit not matching `*/.the-agency/tickets/*` with exit code 2. Built into the app — existing installations get enforcement on next launch.
