# Fill in package.json metadata

## Priority: Low

## Description

`package.json` has empty `description`, `keywords`, and `author` fields — leftover from `npm init -y`. This should be filled in to properly identify the project.

## Tasks

- Add a meaningful `description`
- Add relevant `keywords`
- Add `author` information
- Consider adding `repository`, `license`, and `bugs` fields
